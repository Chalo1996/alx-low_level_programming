#include "main.h"

/**
  * _islower - check whether a character is lower or not.
  *
  * Return: Zero(Success).
  */
int _islower(int c)
{
	int r;

	if (islower(r))
		_putchar('1');
	else
		_putchar('0');
	putchar('\n');
	return (0);
}
