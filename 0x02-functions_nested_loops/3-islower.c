#include "main.h"

/**
  * _islower - check whether a character is lower or not.
  *
  * Return: Zero(Success).
  */
int _islower(int c)
{
	int r;

	if (islower(r))
		return (1);
	else
		return (0);
	putchar('\n');
	return (0);
}
