#include "main.h"

/**
  * _islower - check whether a character is lower or not.
  *
  * Return: Zero(Success).
  */
int _islower(int c)
{
	int r;

	if (islower(r))
		putchar('49');
	else
		putchar('48');
	putchar('\n');
	return (0);
}
